{
  "name": "@100x/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared contracts, API client and theme for the 100x backend, web and mobile apps.",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "~7.0.2",
    "vitest": "^4.1.11"
  }
}
