# Local compose overrides. Copy to .env if you need them — never commit .env.
# Secrets stay in the process environment; they are not mounted as files.

# AUTH_SESSION_SECRET=
# WEB_PORT=3001
# API_PORT=4000
# CORS_ORIGINS=http://localhost:3001,http://127.0.0.1:3001
# API_URL=http://api:8080
